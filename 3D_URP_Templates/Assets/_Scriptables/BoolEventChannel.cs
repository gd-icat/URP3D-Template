using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace sarbajit.icat
{
    [CreateAssetMenu(menuName = "Event Channels/Boolean", fileName = "BEC_New", order = 2)]
    public class BoolEventChannel : EventChannelSOBase<bool>
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
